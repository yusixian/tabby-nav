import { UpdateOptions } from 'react-toastify';

export const toastLoadingEndOpts: UpdateOptions<unknown> = {
  isLoading: false,
  autoClose: 3000,
  hideProgressBar: false,
  closeOnClick: true,
  draggable: true,
  closeButton: true,
};
